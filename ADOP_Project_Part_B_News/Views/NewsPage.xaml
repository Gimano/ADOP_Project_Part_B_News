<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ADOP_Project_Part_B_News.Views.NewsPage"
             Title="NewsPage">
    <VerticalStackLayout>
        <StackLayout>
            <Button Text="Refresh" Clicked="Button_Clicked" />
            <ListView x:Name="GroupedNews" IsGroupingEnabled="true" HasUnevenRows="true">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" Padding="5">
                                <Image Source="{Binding UrlToImage}"/>
                                <StackLayout Orientation="Vertical" >
                                    <Label Text="{Binding DateTime, StringFormat='{0:D}'}"/>
                                    <Label Text="{Binding Description}"/>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </VerticalStackLayout>
</ContentPage>